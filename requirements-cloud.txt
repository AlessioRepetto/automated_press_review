# =============================================================================
# Analisi News — pipeline + report HTML — CLOUD (CPU-only) requirements
#
# Identical to requirements.txt EXCEPT for torch: the cloud runner has no
# GPU, so the CUDA build (torch==2.9.1+cu126) is replaced with the CPU
# build. The CPU wheel is smaller (no bundled CUDA libraries) and is the
# only one installable on a CPU-only runner.
#
# The pipeline logic is identical on CPU — only slower. For a twice-daily
# batch job that is perfectly acceptable.
#
# Keep this file in sync with requirements.txt when versions change.
# The only intentional difference is the torch line.
# =============================================================================

# Core data stack
numpy==2.1.3
pandas==2.2.3
openpyxl==3.1.5
python-dateutil==2.9.0
python-dotenv==1.0.1

# Web and feeds
feedparser==6.0.12
requests==2.32.3
beautifulsoup4==4.12.3

# NLP
nltk==3.9.1
spacy==3.8.11
scikit-learn==1.8.0
# spaCy italian model is installed separately in the workflow, as a wheel:
#   it_core_news_lg-3.8.0  (see daily-report.yml)
keybert==0.9.0

# Embeddings and dimensionality reduction
# --- CPU build of torch (no +cuXXX suffix). Installed from the PyTorch
#     CPU index; see daily-report.yml for the --extra-index-url flag.
torch==2.9.1
sentence-transformers==5.1.0
transformers==4.56.0
umap-learn==0.5.9.post2
hdbscan==0.8.40

# Graphs
networkx==3.4.2

# Visualization
matplotlib==3.10.0
seaborn==0.13.2
adjustText==1.3.0
wordcloud==1.9.4
Pillow==11.0.0
ipython==8.30.0

# LLM
mistralai==1.9.11

# HTML report rendering
jinja2==3.1.4
